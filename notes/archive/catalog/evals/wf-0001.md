---
id: wf-0001
type: eval
for: wf-0001
---

# Eval — Unfamiliar-codebase orientation

## How to re-run

1. Fresh session. Clone `FIXTURE_REPO` at `FIXTURE_SHA` into a clean directory.
2. Apply `cfg-0001`.
3. Execute `wf-0001` steps 2–7 exactly as written, without improvising and without supplying
   knowledge from previous runs. Steps 1 and 8 need humans and are skipped — note that the eval
   therefore does not cover them.
4. Time it. Record the total in the run log.

## Inputs

`FIXTURE_REPO` @ `FIXTURE_SHA`. Nothing else — no README summary, no prior map.

## Pass criteria

- [ ] Every step 3 command runs to completion on the fixture, unmodified. **A command that no
      longer exists or has changed flags is a fail** — this is the most likely way this artifact
      goes stale.
- [ ] The entry-point list (step 4) is complete: no entry point in the fixture is missing.
      Checked against the pinned reference list in `examples/wf-0001.md`.
- [ ] Spot-checking five of the agent's `file:line` citations: all five resolve and say what was
      claimed.
- [ ] The money path (step 5) is traced to the datastore with citations at every hop.
- [ ] `map.md` is produced and names every component in the reference list.
- [ ] The unknowns list has ≥ 5 entries and none of them is answerable from the repository.
- [ ] Total elapsed time is within 150% of the recorded baseline. A workflow that has silently
      doubled in cost has stopped delivering its `outcome`.

## Models to test against

- [ ] frontier model
- [ ] cheaper model

## Run log

| Date | Model | Result | Notes |
|---|---|---|---|
